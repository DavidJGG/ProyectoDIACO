@model IEnumerable<ProyectoDIACO.Models.Comercio>

@{
    ViewData["Title"] = "Index";
}

<h1>Listado de comercios</h1>

<p>
    <a asp-action="Create">Agregar un nuevo comercio</a>
</p>
<table class="table table-striped" id="tablaCom">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefono)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefono)
            </td>
            <td>
                @if(item.Estado==1){
                    <div class="badge bg-success">Activo</div>
                }else{
                    <div class="badge bg-danger">Inactivo</div>
                }
            </td>
            <td>
                <a href="/Sucursals/Index/@item.ComercioId">Sucursales</a> |
                <a asp-action="Edit" asp-route-id="@item.ComercioId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.ComercioId">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.ComercioId">Borrar</a>
            </td>
        </tr>
}
    </tbody>
</table>


<script>
    $(document).ready( function () {
        $('#tablaCom').DataTable({
            language: {
                "decimal":        "",
                "emptyTable":     "No hay resultados",
                "info":           "Mostrando de _START_ a _END_ de _TOTAL_ entradas",
                "infoEmpty":      "Mostrando de 0 a 0 de 0 entradas",
                "infoFiltered":   "(Filtrado un total de _MAX_ entradas)",
                "infoPostFix":    "",
                "thousands":      ",",
                "lengthMenu":     "Mostrar _MENU_ entradas",
                "loadingRecords": "Cargando...",
                "processing":     "",
                "search":         "Buscar:",
                "zeroRecords":    "No se encontraron coincidencias",
                "paginate": {
                    "first":      "Primero",
                    "last":       "Ultimo",
                    "next":       "Siguiente",
                    "previous":   "Anterior"
                },
                "aria": {
                    "sortAscending":  ": Ordenar ascendente",
                    "sortDescending": ": Ordenar descendente"
                }
            }
        });
    } );
</script>
